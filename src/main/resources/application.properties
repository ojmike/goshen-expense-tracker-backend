spring.application.name=goshen-expense-tracker
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# Flyway
spring.flyway.enabled=true
